{
  "name": "telegram-cf-proxy",
  "version": "1.0.0",
  "description": "Drop-in Cloudflare Worker reverse proxy for the Telegram Bot API — bypass region/infrastructure restrictions.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "test": "node examples/_self_test.mjs"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
